<Page
    x:Class="JacobC.Xiami.Views.AlbumPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JacobC.Xiami.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
    xmlns:c="using:JacobC.Xiami.Controls"
    xmlns:vm="using:JacobC.Xiami.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:AlbumViewModel x:Name="ViewModel"/>
    </Page.DataContext>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <controls:PageHeader x:Name="pageHeader" Grid.Row="0" Text="专辑"/>
        <StackPanel Grid.Row="1" MaxWidth="{StaticResource ContentMaxWidth}" BorderThickness="4" BorderBrush="Black">
            <TextBlock FontSize="28" FontWeight="Bold" Text="{x:Bind ViewModel.Album.Name}"/>
            <TextBlock FontSize="20" Text="{x:Bind ViewModel.Album.Description}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <c:ImageEx Width="200" Height="200" Source="{x:Bind ViewModel.Album.ArtFull}"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
